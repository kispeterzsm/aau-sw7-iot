# ===============================
# Application Name
# ===============================
spring.application.name=middleware

# ===============================
# Server Port
# (Python: PORT env, default 8080)
# ===============================
server.port=8080

# ===============================
# PostgreSQL Database (Render)
# From:
# postgresql+asyncpg://iot_zay4_user:DxrBBCLrmwYWGveqLdBRHDwYjgYjfWqM
#   @dpg-d4ecckidbo4c73dee37g-a.frankfurt-postgres.render.com:5432/iot_zay4
# ===============================
spring.datasource.url=jdbc:postgresql://dpg-d4ecckidbo4c73dee37g-a.frankfurt-postgres.render.com:5432/iot_zay4
spring.datasource.username=iot_zay4_user
spring.datasource.password=DxrBBCLrmwYWGveqLdBRHDwYjgYjfWqM
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# Flyway (optional, since you added it)
# ===============================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# ===============================
# Downstream API Settings
# Mirrors Python defaults and env names
# ===============================
# Python: DOWNSTREAM_BASE_URL (default same as below)
downstream.base-url=https://retaliatory-bruna-unofficious.ngrok-free.dev

# Python: DOWNSTREAM_PATH env is shared, but defaults differ there.
# We keep separate props; env can override via DOWNSTREAM_LINK_PATH / DOWNSTREAM_TEXT_PATH
downstream.link-path=/link/all
downstream.text-path=/text/all

# Python: DOWNSTREAM_TIMEOUT_SEC (float, default 30)
downstream.timeout-sec=30

# Python: DOWNSTREAM_AUTH_HEADER / TOKEN
downstream.auth-header=x-auth-token
downstream.auth-token=zlggxclDYTWevcy4zGZeId_M2CUIMKR965fddaD1lvChhqpo9JtKdo

# ===============================
# Extra: DB init retry seconds (if you want to reuse this logic)
# ===============================
db.init-retry-seconds=45

# ===============================
# Logging
# ===============================
logging.level.org.springframework.web=INFO
logging.level.com.example.mi
spring.jackson.property-naming-strategy=SNAKE_CASE